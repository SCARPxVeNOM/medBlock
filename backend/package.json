{
  "name": "medblock-backend",
  "version": "1.0.0",
  "description": "Backend services for healthcare data collaboration platform",
  "main": "uploader.js",
  "scripts": {
    "start:uploader": "node uploader.js",
    "start:keyservice": "node keyservice.js",
    "start:all": "node -e \"require('child_process').spawn('node', ['uploader.js'], {env: {...process.env, PORT: '3001'}, stdio: 'inherit', shell: true}); setTimeout(() => require('child_process').spawn('node', ['keyservice.js'], {env: {...process.env, PORT: '3002'}, stdio: 'inherit', shell: true}), 2000);\"",
    "test": "mocha tests/**/*.test.js --timeout 10000",
    "test:crypto": "mocha tests/crypto.test.js",
    "test:chaincode": "mocha tests/chaincode.test.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "fabric-network": "^2.2.20",
    "js-yaml": "^4.1.0",
    "minio": "^7.1.3",
    "mongoose": "^9.0.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "mocha": "^10.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
